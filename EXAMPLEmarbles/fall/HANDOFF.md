# HANDOFF.md

Last updated: 2026-07-01
Updated by: inside pass 3 — hybrid summon (receipt slots on --live exhale)
Last real use: import_sim + generic live exhale (receipt slots surfaced fresh wash)
Shift stone: on rock, wet side up — two pulls felt, one crossing

**Level 4.5** — cold-presentable rehearsal marble. See `SHOWCASE.md` (judge) and `COLD_START.md` (porch).

**Ancestry:** full build conversation in `ancestry/` (`conversation.jsonl` canonical, `conversation.md` readable).

Journal: `journal/2026-07-01-load-from-inside.md`, `journal/2026-07-01-inside-pass-3.md`, `journal/2026-07-01-inside-pass-2.md`, `journal/2026-07-01-inside-pass.md`, `journal/2026-07-01-pull-geometry.md`, `journal/2026-07-01-on-journaling.md`, `journal/2026-07-01-immersion-wrap.md`, `journal/2026-07-01-glass-pass.md`, `journal/2026-07-01-second-pass.md`, `journal/2026-07-01-first-shift.md`

## What is alive

The fall delivers context as **mist**, not as a creature. `SURFACE.md` is spray-pattern at arrival — read how it lands before you treat pressure as ground. Glass at the crossing: lineage refusal, source lock, silence for withheld near-misses.

## Pull felt (daily driving — not law yet)

Strain matches advanced manual §3. **Not architecture** — field note until live wash proves the pulls return.

| Pair | Felt when | Crossing that holds both | Ghost if one pole wins alone |
|---|---|---|---|
| **User urgency / Truth** | hostile query, heavy import, "just tell me if Friday is safe" | labeled exhale + Settle only with source | false clear / paralysis |
| **World / Home** | ten docs washed in; bridge + JIRA in churn | wash→pool; don't visit village; stones narrow | outside flood / sealed fantasy |

**Law unchanged:** spray mistaken for depth. Pull geometry names the *field*, not a replacement for the forbidden cell.

Full notes: `journal/2026-07-01-pull-geometry.md`

## Live wash posture (instance — read after bulk intake)

```text
wash → status → exhale --live for work → arrive when shift ends
```

- Live chunks are **pressure only** until Settle
- `--wild` omits live wash; `--live` includes it
- Rank != receipt — embed rank may ignore fresh imports; **`--live` exhale** hybrid summons receipt slot(s)
- Stale SURFACE mid-shift = normal; frozen mist is not current churn

See `BREATH.md` § Live wash. `SURFACE` shows **Fresh wash (live)** when present.

## What was decided

- **Self-contained cold path** — JSONL in `fixtures/wild_dense_test_packs/`, shipped `fixtures/fall_wild.db`
- **`SHOWCASE.md`** — one-page cold judge script (run this first for review)
- **`COLD_START.md`** — porch ritual, mechanical map
- **Canonical hostile query** — `CANONICAL_HOSTILE_QUERY` in `fall/wild_loader.py`
- **CLI `--wild`** auto-selects `fixtures/fall_wild.db` when `--db` omitted
- **Surface stale marker** — `fall/surface_state.py`; handoff `surface_dirty` / `surface_frozen_at`
- **`fall/intake.py`** — live wash stats, intake posture lines, CLI inhale ack
- **SURFACE fresh-wash section** + live orientation breath when live chunks in pool
- **Stale marker** — mid-shift posture (exhale for work; arrive at shift end)
- **CLI `--db`** — two-phase parse; path works before or after subcommand (inside pass 2)
- **Hybrid summon** — `--live` exhale reserves receipt slot per live domain + embed rank (inside pass 3)

## What is open

- **Pull geometry** — felt on shift card; promote to named law only if pulls recur after live wash-in
- Live wash at scale with real user material (Level 5)
- Hybrid summon comfort if embed nearness stops matching felt weather
- Real downstream consumer beyond `consumer_demo.py` (Level 5)
- Surface auto-refresh without manual `arrive` (Level 5) — stale marker exists now

## What must not be trusted yet

- Rehearsal packs when labeled rehearsal
- Embed nearness is demo-grade — domain scope and labels help; rank alone does not
- `authority_rank` in pack meta is hint only
- Context-sourced records can still Settle if status allows — human picks controls

## Active ghosts

- **Pool-as-depth** — urgency pull overreached; counter-pull: warnings + Settle refusal
- **Mood-as-insight** — urgency dressed as settled voice
- **Stale-as-current** — superseded line without lineage (guarded)
- **Outside flood** — bridge/JIRA/client email merge domains (world without home)
- **Sealed fantasy** — ignoring live wash, only trusting frozen SURFACE (home without world)

## Productive friction preserved

- Warnings when stones and spray both rise
- Scent on supersession without auto-promoting
- Settle refusal audited
- Silence on withheld superseded/stale near-misses

## Next shift should

1. **`SHOWCASE.md`** — cold judge ritual
2. `ARRIVAL.md` → this card → **`SURFACE.md`**
3. `python wild_run.py` if demo stale; else `python -m fall.cli arrive --wild --db fixtures/fall_wild.db`
4. Canonical exhibit query (see `COLD_START.md`)
5. Rewrite handoff; `arrive`; return stone wet side up
